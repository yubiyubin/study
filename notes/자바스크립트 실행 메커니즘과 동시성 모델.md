# 자바스크립트 실행 메커니즘과 동시성 모델

![image.png](%EC%9E%90%EB%B0%94%EC%8A%A4%ED%81%AC%EB%A6%BD%ED%8A%B8%20%EC%8B%A4%ED%96%89%20%EB%A9%94%EC%BB%A4%EB%8B%88%EC%A6%98%EA%B3%BC%20%EB%8F%99%EC%8B%9C%EC%84%B1%20%EB%AA%A8%EB%8D%B8/image.png)

Call Stack (JS 엔진 영역)

- JS 코드가 **실제로 실행되는 유일한 장소**
- 함수 호출 → push
- 함수 종료 → pop
- **싱글 스레드**

```jsx
foo();
bar();

```

👉 무조건 여기서만 실행됨

---

### ② Web API (브라우저 영역)

- JS 엔진 **밖**
- 시간이 걸리는 일 담당

예:

- `setTimeout`
- `fetch`
- 이벤트 감지
- DOM 이벤트 대기

📌 Web API는 **실행하지 않는다**

→ “기다리고 있다가 완료되면 알림만 보냄”

---

### ③ Task Queue / Microtask Queue (대기실)

### Task Queue

- `setTimeout`
- DOM 이벤트
- `setInterval`

### Microtask Queue

- `Promise.then`
- `queueMicrotask`
- `MutationObserver`

📌 **Microtask가 Task보다 우선**

---

### ④ Event Loop (조정자)

Event Loop가 하는 일은 딱 하나예요.

> “Call Stack이 비었는지 계속 확인”
> 
- 비었으면?
    - Microtask Queue 먼저 비움
    - 그다음 Task Queue 하나 꺼냄
- 안 비었으면?
    - 아무것도 안 함

👉 Event Loop는 **코드를 실행하지 않음**

👉 “언제 실행할지”만 결정

---

## 3️⃣ 전체 흐름을 한 사이클로 보면

### 예제

```jsx
console.log("A");

setTimeout(() => {
console.log("B");
},0);

Promise.resolve().then(() => {
console.log("C");
});

console.log("D");

```

### 실제 흐름

1️⃣ `console.log("A")` → Call Stack

2️⃣ `setTimeout` → Web API로 위임

3️⃣ `Promise.then` → Microtask Queue 등록

4️⃣ `console.log("D")` → Call Stack

(Call Stack 비어짐)

5️⃣ Event Loop 확인

6️⃣ Microtask Queue → `"C"` 실행

7️⃣ Task Queue → `"B"` 실행

👉 결과:

### 

```
A
D
C
B

```