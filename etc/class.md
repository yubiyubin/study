# class

> class는 **같은 구조를 가진 객체를 반복해서 만들기 위한 템플릿**이다!
> 

<aside>
💡

데이터와 행동(메서드)을 묶어서 하나의 구조에 정의해두고, 필요할 때 `new` 사용해서 여러 객체를 계속 찍어낼 수 있다! 

</aside>

# 1) class 구성 요소

## 1-1)  constructor

> 이 class로 객체가 만들어질 때 자동 실행되는 함수
> 

```jsx
constructor(name) {
  this.name = name;
}
```

## 1-2) this

> 생성된 객체 **자기 자신**을 가리킨다.
> 

```jsx
this.name = name;
```

## 1-3) 메서드

> class 내부에 선언된 함수 ⇒  공통으로 사용할 행동을 정의한다.
> 

```jsx
sayHi() {
  console.log(`안녕 나는 ${this.name}`);
}
```

## 1-4) 인스턴스 생성: `new`

> class는 그냥 설계도고, 설계도를 기반으로 진짜 객체를 찍어낼 때 `new`를 사용한다.
> 

```jsx
const p = new Person("민수", 30);
```

---

# 2) class 기본 형태

```jsx
class Person {
  constructor(name, age) {
    this.name = name;   // 인스턴스 프로퍼티(데이터)
    this.age = age;
  }

  introduce() {         // 메서드(행동)
    console.log(`${this.name} (${this.age})`);
  }
}

const me = new Person("유진", 24);  // 인스턴스 생성
me.introduce();

```